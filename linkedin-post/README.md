# LinkedIn Post Screenshots

Run the app locally, then generate the screenshots:

```bash
npm run dev
node linkedin-post/capture-linkedin-screenshots.mjs
```

The script mocks the story and AI API responses so it does not use real AI quota.
